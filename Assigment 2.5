#include <stdio.h>

int main() {
   float g,d,n;
   printf("enter gross income");
   scanf("%f",&g);
   if(g>20000)
   d=0.15*g;
   else if(g>10000)
d=0.1*g;
else
d=0.05*g;
n=g-d;
printf("net price is %f.2",n);
    return 0;
}